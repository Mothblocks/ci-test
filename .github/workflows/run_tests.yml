name: CI Suite
on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master
jobs:
  run_tests:
    name: Run Tests
    runs-on: ubuntu-20.04
    steps:
      - name: Pass
        run: echo Looks good
